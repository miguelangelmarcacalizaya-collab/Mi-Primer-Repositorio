import java.util.Scanner;

public class PilaDeStrings {
    private String[] pila;
    private int tope;
    private int tamaño;

    // Constructor
    public PilaDeStrings(int tamaño) {
        this.tamaño = tamaño;
        pila = new String[tamaño];
        tope = -1;
    }

    // Insertar un elemento
    public void push(String dato) {
        if (isFull()) {
            System.out.println("❌ La pila está llena.");
        } else {
            pila[++tope] = dato;
            System.out.println("✔️ Se agregó: " + dato);
        }
    }

    // Eliminar un elemento
    public String pop() {
        if (isEmpty()) {
            System.out.println("⚠️ La pila está vacía.");
            return null;
        } else {
            String dato = pila[tope--];
            System.out.println("🗑️ Se eliminó: " + dato);
            return dato;
        }
    }

    // Ver elemento superior
    public String peek() {
        if (isEmpty()) {
            System.out.println("⚠️ La pila está vacía.");
            return null;
        } else {
            return pila[tope];
        }
    }

    // Mostrar todos los elementos
    public void mostrar() {
        if (isEmpty()) {
            System.out.println("⚠️ No hay elementos en la pila.");
        } else {
            System.out.println("\n📦 Contenido de la pila:");
            for (int i = tope; i >= 0; i--) {
                System.out.println("[" + i + "] " + pila[i]);
            }
        }
    }

    // Verificar si está vacía
    public boolean isEmpty() {
        return tope == -1;
    }

    // Verificar si está llena
    public boolean isFull() {
        return tope == tamaño - 1;
    }

    // Programa principal con menú
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        PilaDeStrings pila = new PilaDeStrings(5);
        int opcion;

        do {
            System.out.println("\n--- MENÚ PILA ESTÁTICA ---");
            System.out.println("1. Insertar palabra (push)");
            System.out.println("2. Eliminar palabra (pop)");
            System.out.println("3. Ver cima (peek)");
            System.out.println("4. Mostrar pila");
            System.out.println("5. Salir");
            System.out.print("Elige una opción: ");
            opcion = sc.nextInt();
            sc.nextLine(); // limpiar buffer

            switch (opcion) {
                case 1:
                    System.out.print("Escribe la palabra: ");
                    String palabra = sc.nextLine();
                    pila.push(palabra);
                    break;
                case 2:
                    pila.pop();
                    break;
                case 3:
                    String cima = pila.peek();
                    if (cima != null) System.out.println("👀 En la cima está: " + cima);
                    break;
                case 4:
                    pila.mostrar();
                    break;
                case 5:
                    System.out.println("👋 Saliendo del programa...");
                    break;
                default:
                    System.out.println("❌ Opción no válida.");
            }
        } while (opcion != 5);
        sc.close();
    }
}
